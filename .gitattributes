# Normalise line endings: LF in the repository, native in the working tree.
# Keeps Prettier's `--check` deterministic between Windows dev and Linux CI.
* text=auto eol=lf

# Binary assets must never be normalised.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
